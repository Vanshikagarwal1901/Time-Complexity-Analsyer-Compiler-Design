[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tca"
version = "0.1.0"
description = "Time complexity analyzer (compiler-design style)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
tca = "tca.cli:main"
